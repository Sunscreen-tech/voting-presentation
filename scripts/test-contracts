#!/usr/bin/env bash
# Extract and test Solidity contracts (wrapper for backwards compatibility)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/contracts" test
